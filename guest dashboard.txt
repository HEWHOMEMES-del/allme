import React from 'react';
import { View, Text, TouchableOpacity, StyleSheet, ScrollView } from 'react-native';

export default function GuestScreen() {
  return (
    <ScrollView contentContainerStyle={styles.container}>
      <Text style={styles.welcome}>Welcome, Guest!</Text>

      <Text style={styles.infoText}>
        You have limited access. To unlock full features and exclusive Matric revision content,
        please create an account or log in.
      </Text>

      <TouchableOpacity style={styles.button}>
        <Text style={styles.buttonText}>Browse Free Lessons</Text>
      </TouchableOpacity>

      <TouchableOpacity style={styles.button}>
        <Text style={styles.buttonText}>View Sample Past Papers</Text>
      </TouchableOpacity>

      <TouchableOpacity style={styles.loginButton}>
        <Text style={styles.loginButtonText}>Login / Signup</Text>
      </TouchableOpacity>
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container: { 
    padding: 20, 
    backgroundColor: '#f5f5f5', 
    alignItems: 'center',
    minHeight: '100%',
  },
  welcome: {
    fontSize: 28,
    fontWeight: 'bold',
    marginBottom: 15,
    color: '#555',
  },
  infoText: {
    fontSize: 16,
    marginVertical: 15,
    color: '#666',
    textAlign: 'center',
    paddingHorizontal: 10,
  },
  button: {
    backgroundColor: '#999',
    width: '85%',
    padding: 15,
    borderRadius: 10,
    marginVertical: 8,
  },
  buttonText: {
    color: 'white',
    fontWeight: '600',
    textAlign: 'center',
  },
  loginButton: {
    marginTop: 25,
  },
  loginButtonText: {
    color: '#3f3fff',
    fontWeight: '700',
    fontSize: 18,
  },
});
