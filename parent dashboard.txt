import React from 'react';
import { View, Text, TouchableOpacity, StyleSheet, ScrollView } from 'react-native';

export default function ParentDashboard() {
  return (
    <ScrollView contentContainerStyle={styles.container}>
      <Text style={styles.welcome}>Welcome, Parent!</Text>

      <TouchableOpacity style={styles.card}>
        <Text style={styles.cardTitle}>📊 Child’s Progress Overview</Text>
      </TouchableOpacity>

      <TouchableOpacity style={styles.card}>
        <Text style={styles.cardTitle}>🗓️ Upcoming Exams & Events</Text>
      </TouchableOpacity>

      <TouchableOpacity style={styles.card}>
        <Text style={styles.cardTitle}>📄 Download Reports</Text>
      </TouchableOpacity>

      <TouchableOpacity style={styles.card}>
        <Text style={styles.cardTitle}>✉️ Messages from Teachers</Text>
      </TouchableOpacity>

      <TouchableOpacity style={styles.card}>
        <Text style={styles.cardTitle}>⚙️ Profile & Settings</Text>
      </TouchableOpacity>
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container: { 
    padding: 20, 
    backgroundColor: '#fff7f3', 
    alignItems: 'center',
    minHeight: '100%',
  },
  welcome: {
    fontSize: 28,
    fontWeight: 'bold',
    marginBottom: 25,
    color: '#ff5e3e',
  },
  card: {
    backgroundColor: '#ff8a7f',
    width: '90%',
    padding: 20,
    marginVertical: 10,
    borderRadius: 15,
    shadowColor: '#ff5200',
    shadowOpacity: 0.3,
    shadowRadius: 5,
  },
  cardTitle: {
    fontSize: 20,
    fontWeight: '600',
    color: 'white',
    textAlign: 'center',
  },
});
